// * Coding Challenge #1
/*
// TODO 1.Store Mark's and John's mass and height in variables
// const marksWeight = 78,
//     johnWeight = 92;
// const marksHeight = 1.69,
//     johnHeight = 1.95;

const marksWeight = 95,
    johnWeight = 85;
const marksHeight = 1.88,
    johnHeight = 1.76;

// TODO 2.Calculate both their BMIs using the formula (you can even implement both versions)
const marksBMI = marksWeight / marksHeight ** 2;
const johnBMI = johnWeight / johnHeight ** 2;

const markHigherBMI = marksBMI > johnBMI;

// TODO 3.Create a Booleanvariable 'markHigherBMI'containing information about whether Mark has a higher BMI than John.
console.log(marksBMI, johnBMI, markHigherBMI);
*/

// FIM

// * Coding Challenge #2
/*
// const marksWeight = 78,
//     johnWeight = 92;
// const marksHeight = 1.69,
//     johnHeight = 1.95;

const marksWeight = 95,
    johnWeight = 85;
const marksHeight = 1.88,
    johnHeight = 1.76;

const marksBMI = marksWeight / marksHeight ** 2;
const johnBMI = johnWeight / johnHeight ** 2;

if (marksBMI > johnBMI) {
    console.log(`Mark's BMI (${marksBMI}) is higher than John's (${johnBMI})!`);
} else {
    console.log(`John's BMI (${johnBMI}) is higher than Mark's (${marksBMI})!`);
}
*/

// FIM

// * Coding Challenge #3
/*
// Data 1: Dolphins score 96, 108 and 89. Koalas score 88, 91 and 110
// Data Bonus1: Dolphins score 97, 112 and 101. Koalas score 109, 95 and 123
// Data Bonus2: Dolphins score 97, 112 and 101. Koalas score 109, 95 and 106

const dolphins = (97 + 112 + 101) / 3;
const koalas = (109 + 95 + 106) / 3;

if (dolphins > koalas && dolphins >= 100) {
    console.log("Dolphins Wins! 🏆 🐬");
} else if (koalas > dolphins && koalas >= 100) {
    console.log("Koalas Wins! 🏆 🐨");
} else if (dolphins === koalas && dolphins >= 100 && koalas >= 100) {
    console.log("Draw 😮");
} else {
    console.log("Nobody wins. 🙁");
}

 */
// FIM

// * Coding Challenge #4
/*
const bill = 275;
const tip = bill >= 50 && bill <= 300 ? 1.15 : 1.2;
const finalBill = bill * tip;

console.log(
    `The bill was ${bill}, the tip was ${
        finalBill - bill
    }, and the total value ${finalBill}`
);

*/

// FIM
